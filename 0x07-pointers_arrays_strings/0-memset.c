#include "main.h"
/**
 * _memset - a function that fills memory with a constant byte
 * @s: a poitner to destination
 * @b: to be filled to memory
 * @n: size of bytes to be filled
 * Return:address
 */
char *_memset(char *s, char b, unsigned int n)
{
	while (n--)
	{
		*s = c;
	}
	return (s);
}

